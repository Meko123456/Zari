<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Recording the call audio. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Knowing a call started and ended. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
      The number and whether the call was incoming or outgoing. Since Android 10 the PHONE_STATE
      broadcast no longer carries the number unless the app holds READ_CALL_LOG, and reading the
      newest call-log entry after the call is more reliable than the deprecated broadcast extras
      anyway - it also gives the network's own duration to check the recording against.
    -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- Turning a number into a name. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Not for drawing anything. Android 12+ forbids starting a foreground service from the
      background, and holding SYSTEM_ALERT_WINDOW is one of the documented exemptions - without
      it the service cannot start from the PHONE_STATE broadcast, which is the only moment we
      know a call began. The app never actually draws an overlay.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zari">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Zari">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".call.RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".call.PhoneStateReceiver"
            android:exported="true"
            android:permission="android.permission.READ_PHONE_STATE">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
